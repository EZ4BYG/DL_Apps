1. 直接下载整个区域的geotiff文件（定位用），然后备份2张对应的.png图像（原图 + 标签）

2. 在PS中处理左边1/3的.png图像（训练集 + 测试集）

3. 直接用程序切割每张子图为256x256：用整除的方式，右边不够256的就小一点没关系（resize）

4. 最后用程序再合并

说明：所有的处理都是在png上；最初的.tiff文件只为了最终的定位！

--- 
大图分割后的png：该文件夹只是文件的中转站，并无大用！—— 最后要用模型，对该文件夹内的所有文件进行预测，获得每张图对应的“预测标签图”！“预测标签图”才是最后需要合并的。

数据集：两个文件夹东西一样，对标签文件夹内图像再做ps标定！
- 测试1：数据集总量100，直接取前100个（无随机）；